{
  "rules": {
    ".read": false,
    ".write": false,
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        ".write": "auth != null && auth.uid == $uid",
        "sessions": {
          ".indexOn": ["updatedAt"],
          "$sessionId": {
            "messages": {
              ".indexOn": ["createdAt"]
            }
          }
        }
      }
    }
  }
}
